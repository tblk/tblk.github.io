---
title: "asciinema"
date: 2023-11-03
execute: 
  freeze: false
---

```{r echo = FALSE, results = "hide", purl = FALSE}
asciicast::init_knitr_engine()
```

```{r}
library(cli)
```

```{r}
wait <- function(seconds = 2) {
  Sys.sleep(seconds)
}
```

```{asciicast}
send_cli_threat <- function() {
  cli::cli_text("Dead girls walking."); wait()
  cli::cli_text("--A.")
}
send_cli_threat()
```